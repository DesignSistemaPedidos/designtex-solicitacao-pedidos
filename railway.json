{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "FLASK_APP=app flask run -p 8080",
    "healthcheckPath": "/health"
  },
  "env": {
    "ENVIRONMENT": "production"
  }
}