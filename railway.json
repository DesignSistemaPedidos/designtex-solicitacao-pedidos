{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "web: gunicorn -w 2 -k gthread -t 120 -b 0.0.0.0:$PORT app:app",
    "healthcheckPath": "/health"
  },
  "env": {
    "ENVIRONMENT": "production"
  }
}
