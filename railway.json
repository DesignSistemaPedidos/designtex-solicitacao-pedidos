{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "gunicorn -w 2 -k gthread -t 120 -b 0.0.0.0:8080 app:app",
    "healthcheckPath": "/health"
  },
  "env": {
    "ENVIRONMENT": "production"
  }
}
